# Server
PORT=4141
LOG_LEVEL=info

# GitHub token used in config.yaml (device-flow or PAT with Copilot scope)
GITHUB_TOKEN=

# Optional master key (when set, clients must send `Authorization: Bearer <MASTER_KEY>`)
MASTER_KEY=
